# Bare Bone backend for connecting multiple node servers together into a single point to be able to access them! From a single location request!

# Basic load balancing implementations on the client request for avaliable node servers! to help the node servers out and clients to ensure they aren't sent to full servers!

# 3 Way Verification for video relay nodes utilizing merkle roots of the process current working directory with exclusions on the tmp folder and configurations folder!

# Obfuscated this checksum generation process to help secure the video relay node servers!

# Obfuscated the storage of the secret environment variables as well as stored as the base64 value instead Later might utilize sha256 hash comparisions!

# Created The structure for Cloudflare handling along with a waitlist system to queue them for generating there subdomain!

# Implemented my old IP flagging system to help protect our cloudflare service from attack vectors just need to actually implement the call of the function as well as testing to make sure we dont need to update it to async properly!

